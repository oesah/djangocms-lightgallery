============
Installation
============

At the command line::

    $ easy_install djangocms_lightgallery

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv djangocms_lightgallery
    $ pip install djangocms_lightgallery
